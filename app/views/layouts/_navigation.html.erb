<div class="nav-collapse">
    <%= link_to "Just a reminder", root_path, :class => 'brand' %>
    <ul class="nav">
      <% if current_user %>
          <li>
            <%= link_to('Logout', logout_path, :method=>'delete') %>
          </li>
      <% else %>
          <li>
            <%= link_to('Login', login_path)  %>
          </li>
      <% end %>
      <% if current_user %>
          <li>
            <%= link_to('Edit account', login_path) %>
          </li>
      <% else %>
          <li>
            <%= link_to('Sign up', login_path)  %>
          </li>
      <% end %>
    </ul>
</div>