<div class="nav-collapse">
    <%= link_to image_tag("/assets/Reminder_Icon_50.png", :border => 0) + " Just a reminder", {:action => 'index', :controller => 'pages'}, :class => 'brand'%>
    <ul class="nav">
      <% if current_user %>
      	  <li>
            <%= link_to('Calendar', reminder_calendar_path) %>
          </li>
          <li class="dropdown">
          	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Contacts <b class="caret"></b></a>
            <ul class="dropdown-menu">
            	<li><%= link_to('View Your Contacts', contacts_path) %></li>
            	<li><%= link_to('Add a Contact', new_contact_path) %></li>
            </ul>
          </li>
          <li class="dropdown">
          	<a href="#" class="dropdown-toggle" data-toggle="dropdown">Reminders <b class="caret"></b></a>
            <ul class="dropdown-menu">
            	<li><%= link_to('View Your Reminders', reminders_path) %></li>
            	<li><%= link_to('Add a Reminder', new_reminder_path) %></li>
            </ul>
          </li>
      <% else %>
          <li>
            <%= link_to('Login', login_path)  %>
          </li>
      <% end %>
      <% if current_user %>
          <li>
            <%= link_to('Edit account', edit_user_path(current_user)) %>
          </li>
          <li>
          	<%= link_to('Logout', logout_path) %>
          </li>
      <% else %>
          <li>
            <%= link_to('Sign up', new_user_path)  %>
          </li>
      <% end %>
    </ul>
</div>